{% extends 'base.html.twig' %}

{% block body %}

<div class="div-titre">
        <a href="{{ path('app_loyers') }}" class="btn-retour">Retour</a>
        <h1>Modifier une charge</h1>
</div>

<form method="post">

    <div>
        <label>Montant</label>
        <input type="number" name="montant" value="{{ loyer.montant }}">
    </div>

    <div>
        <label>Date</label>
        <input type="date" name="date" value="{{ loyer.dateMES ? loyer.dateMES|date('Y-m-d') : '' }}">
    </div>

    <button type="submit">Enregistrer</button>

</form>

<link rel="stylesheet" href="{{ asset('css/style_form.css') }}">
<link rel="stylesheet" href="{{ asset('css/style_titre.css') }}">

{% endblock %}